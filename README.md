# Configuration

Prerequisites:
```
node -v
npm -v
```
If Node.js version < 20.0, update version to LTS: 
```
nvm ls
nvm install --lts
nvm use (lts-version-number)
```

Angular install:
```
npm install -g @angular/cli
ng version
```
